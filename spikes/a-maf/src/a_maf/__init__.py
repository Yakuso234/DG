"""SPIKE-001 Spike A — Microsoft Agent Framework (MAF) 1.0 minimal prototype.

Implementation ("glue") code for the acceptance scenarios S1–S5. This package
reuses the shared domain core at ``spikes/shared/domain.py`` (added to
``sys.path`` by ``tests/conftest.py``) and does not re-implement its semantics.
"""

__version__ = "0.1.0"
